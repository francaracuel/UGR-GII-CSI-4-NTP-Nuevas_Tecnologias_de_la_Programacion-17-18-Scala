Pasos:

1. Constructor: argumento: ruta archivo datos.txt

	1. crear un hashmap con dni, empleado
	2. leer las líneas como en el fichero de ejemplo
	3. por cada linea se trocean los datos y se llama a crearempleado. se usa el patron de ayer utilizando la , para separar. 
		cuando se haya separado se llama al constructor de empleado. los empleados se meten en listadoArchivo (no listado).



2. obtenerNumeroEmpleadosArchivo
- debe haber 5000


3. hayDnisRepetidosArchivo
- debería salir true (2 dnis repetidos la primera vez con 2 empleados en cada uno (4 empleados))

4. a: obtenerDnisRepetidosArchivo
- salen los 4 repetidos. El método devuelve un mapa<String, List<Empleado>> 


b: contarDnisRepetidos:
- debería devolver 4


5. repararDnisRepetidos:
- recibe el mapa del 4.a. se usa el método dniAleatorio para cambiarle el dni.


6. hayCorreosRepetidosArchivo:
- devuelve true o false. la primera vez si hay repetidos.

7. a: obtenerCorreosRepetidos:
- igual que como se hace en el punto 4 pero con los correos

7b: contarCorreosRepetidos:
- debería haber 315.

8. repararCorreosRepetidos:
- recibe el mapa del 7a y llama a generarCorreoCompleto() para arreglarlo

9 validarListaArchivo:
- se pasa toda la información a listado.





